parameters:
  - description: Destination where items will be replaced.
    enum:
      - loads
      - trucks
      - private_loads
      - private_trucks
      - test_loads
      - test_trucks
      - live_loads_temp
      - test_loads_temp
      - live_trucks_temp
      - test_trucks_temp
    in: path
    name: posting_type
    required: true
    type: string
post:
  consumes: []
  parameters:
    - in: header
      name: end-user-token
      required: true
      type: string
    - in: body
      name: body
      required: true
      schema:
        $ref: '#/definitions/post_posting_replace_all_body'
  responses:
    '202':
      description: Success
      schema:
        $ref: '#/definitions/postings-replace_all_response'
  summary: >-
    Will delete all original items in the destination after moving all loads in
    the source to the destination
  tags:
    - postings
x-restlet:
  section: POSTINGS
