delete:
  parameters:
    - description: Delete data from a custom field.
      in: query
      items:
        type: string
      name: posting_id
      required: true
      type: array
    - in: header
      name: end-user-token
      required: true
      type: string
  responses:
    '204':
      description: 200 OK
  summary: 'STAGE=UNK Delete some or all custom columns, or custom_column data.'
  tags:
    - postings/custom_fields
get:
  parameters:
    - description: To just return information about some postings.
      in: query
      items:
        type: string
      name: posting_id
      required: true
      type: array
    - in: header
      name: end-user-token
      required: true
      type: string
  responses:
    '200':
      description: successful operation
      schema:
        $ref: '#/definitions/get_custom_fields_response'
  summary: 'STAGE=UNK Get a list of custom columns, or custom_column data.'
  tags:
    - postings/custom_fields
patch:
  parameters:
    - description: To just return information about some postings.
      in: query
      items:
        type: string
      name: posting_id
      required: true
      type: array
    - in: header
      name: end-user-token
      required: true
      type: string
  responses:
    '204':
      description: successful operation
  summary: STAGE=UNK Update custom field data.
  tags:
    - postings/custom_fields
post:
  parameters:
    - in: header
      name: end-user-token
      required: true
      type: string
  responses:
    '201':
      $ref: '#/responses/object_created'
  summary: 'STAGE=UNK Create new category names, or add new data.'
  tags:
    - postings/custom_fields
x-restlet:
  section: POSTINGS/CUSTOM_FIELDS
