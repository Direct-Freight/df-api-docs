get:
  parameters:
    - format: float
      in: query
      name: latitude
      required: true
      type: number
    - format: float
      in: query
      name: longitude
      required: true
      type: number
  responses:
    '200':
      description: successful operation
      schema:
        $ref: '#/definitions/closest_city_response'
  summary: returns the nearest city to a lat/lon point.
  tags:
    - locations
x-restlet:
  section: LOCATIONS
