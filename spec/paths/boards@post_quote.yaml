post:
  consumes: []
  description: Post a quote for a load.
  parameters:
    - in: header
      name: end-user-token
      required: true
      type: string
    - in: body
      name: body
      required: true
      schema:
        $ref: '#/definitions/quote_partner_load'
  responses:
    '201':
      $ref: '#/responses/object_created'
  summary: STAGE=UNK Post quote for a load.
  tags:
    - boards
x-restlet:
  section: BOARDS
