get:
  description: Get the offer from the broker for this load.  STAGE=Private
  parameters:
    - description: 32 character entry id
      in: query
      name: entry_id
      required: true
      type: string
    - in: header
      name: end-user-token
      required: true
      type: string
  responses:
    '200':
      description: successful operation
      schema:
        $ref: '#/definitions/single_load_response'
  summary: Get Broker Offer
  tags:
    - boards
x-restlet:
  section: BOARDS
