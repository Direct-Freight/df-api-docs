get:
  description: >-
    Get a list of accepted and submitted offers driver has for this load. 
    STAGE=Private
  parameters:
    - description: 32 character entry id
      in: query
      name: entry_id
      required: true
      type: string
  responses:
    '201':
      description: List of offers
      schema:
        $ref: '#/definitions/boards_offer_driver_list_response'
  summary: Get accepted and submitter driver offers
  tags:
    - boards
x-restlet:
  section: BOARDS
