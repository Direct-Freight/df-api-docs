parameters:
  - description: 'Valid types are "loads", and "trucks".'
    enum:
      - loads
      - trucks
    in: path
    name: board_type
    required: true
    type: string
    x-example: loads
post:
  consumes: []
  description: >-
    This is the primary search method for loads and trucks. Please specifify at
    least an origin or destination.  The origin or destination may be state
    only.
  parameters:
    - default: 6c5544b743de1f42f6f9a8a0880f837530ab2eea
      in: header
      name: api-token
      required: true
      type: string
    - default: 01234567890
      in: header
      name: end-user-token
      required: false
      type: string
      x-example: 01234567890
    - default: application/json
      enum:
        - application/json
      in: header
      name: Accept
      required: true
      type: string
      x-example: application/json
    - default: application/json
      enum:
        - application/json
      in: header
      name: Content-Type
      required: true
      type: string
      x-example: application/json
    - in: body
      name: body
      required: true
      schema:
        $ref: '#/definitions/board_request'
  responses:
    '200':
      description: Returns a list matching your search.
      schema:
        $ref: '#/definitions/board_response'
    '422':
      $ref: '#/responses/error_422'
  summary: Retrieve a board
  tags:
    - boards
x-restlet:
  section: BOARDS
