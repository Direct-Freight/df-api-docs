get:
  description: This will return a multi post report. Multiple reports are stored.
  parameters:
    - in: query
      name: report_id
      required: true
      type: string
      x-example: 1cc94d41bcb08de0586a39ae8460a7aa
    - in: header
      name: end-user-token
      required: true
      type: string
  responses:
    '200':
      description: Status 200
      schema:
        properties:
          job_finish:
            description: This will be null until the postings have finished being entered.
            format: date-time
            type: string
          job_start:
            description: When the submission happened.
            format: date-time
            type: string
          list:
            items:
              properties:
                custom_id:
                  description: If submitted will show up in this list.
                  type: integer
                entry_id:
                  description: Will be null on a failed entry.
                  type: string
                message:
                  description: 'If an error occurred, this will explain it.'
                  type: string
              required:
                - entry_id
                - message
              type: object
            type: array
          load_entered:
            description: >-
              A percentage of how many loads were entered. This information
              dynamically changes until the report completes.
            type: number
          success_ratio:
            description: >-
              A percentage of how many loads were entered. This information
              dynamically changes until the report completes.
            type: number
        required:
          - job_finish
          - job_start
          - list
          - load_entered
          - success_ratio
        type: object
  summary: Get a report.
  tags:
    - postings
x-restlet:
  section: POSTINGS
