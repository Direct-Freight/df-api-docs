open Cohttp_lwt_unix
open Cohttp
open Lwt

let uri = Uri.of_string "https://www.directfreight.com/api/profiles/mobile_carriers" in
let body = Cohttp_lwt_body.of_string "{\"carrier_id\":0}" in

Client.call ~body `GET uri
>>= fun (res, body_stream) ->
  (* Do stuff with the result *)