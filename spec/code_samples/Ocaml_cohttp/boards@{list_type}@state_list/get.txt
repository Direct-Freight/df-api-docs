open Cohttp_lwt_unix
open Cohttp
open Lwt

let uri = Uri.of_string "https://www.directfreight.com/api/boards/%7Blist_type%7D/state_list" in

Client.call `GET uri
>>= fun (res, body_stream) ->
  (* Do stuff with the result *)