---
properties:
  email:
    description: ! 'Enter an e-mail to see if it is available. Can
      be used as ?email=xxx or as a JSON request. '
    type: string
  exists:
    description: ! 'Returns a true value if it is associated with
      an account. '
    type: boolean
required:
- email
- exists
