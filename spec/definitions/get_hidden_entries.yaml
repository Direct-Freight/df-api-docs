type: object
properties:
  entry_id:
    type: array
    description: A subset of entries that need to be marked as hidden.
    items:
      type: string
example:
  entry_id:
    - entry_id
    - entry_id
