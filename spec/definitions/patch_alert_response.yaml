---
example:
  alert:
  - ! '{}'
  - ! '{}'
properties:
  alert:
    description: Same as get, but just one item instead of a list of items.
    items:
      properties: {}
      type: object
    type: array
