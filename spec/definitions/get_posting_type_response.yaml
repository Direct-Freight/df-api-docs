type: object
properties:
  posting_list:
    type: array
    description: A list of posting objects you can iterate over.
    items:
      type: string
example:
  posting_list:
    - posting_list
    - posting_list
