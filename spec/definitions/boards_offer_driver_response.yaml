properties:
  driver_contact_info:
    type: string
  load_info_when_quoted:
    properties:
      age:
        format: int32
        type: integer
      allowed_actions:
        type: string
      booking_info:
        properties:
          book_button_action:
            type: string
          book_button_label:
            type: string
          book_header_label:
            type: string
          book_message:
            type: string
          df_reference_code:
            type: string
          quote_button_action:
            type: string
          quote_button_label:
            type: string
          quote_header_label:
            type: string
          quote_message:
            type: string
          show_book_rate:
            format: int32
            type: integer
          show_or_label:
            format: int32
            type: integer
          show_quote_input:
            format: int32
            type: integer
        type: object
      booking_partner:
        type: string
      broker_user_id:
        format: int32
        type: integer
      comment:
        format: nullable
        type: string
      company_name:
        type: string
      credit_report_key:
        type: string
      credit_score:
        format: nullable
        type: string
      custom_id:
        type: string
      destination_city:
        type: string
      destination_state:
        type: string
      details_url:
        format: nullable
        type: string
      entry_id:
        type: string
      full_load:
        type: boolean
      green_light:
        type: boolean
      height:
        format: nullable
        type: string
      length:
        format: nullable
        type: string
      origin_city:
        type: string
      origin_state:
        type: string
      other_trailer_types:
        format: nullable
        type: string
      pay_rate:
        format: int32
        type: integer
      phone_extension:
        format: int32
        type: integer
      phone_number:
        format: int64
        type: integer
      rate_per_mile_est:
        type: number
      receive_date:
        format: nullable
        type: string
      ship_date:
        format: date
        type: string
      tags:
        items:
          type: string
        type: array
      trade_orgs:
        properties:
          tia_approved:
            type: string
        type: object
      trailer_type:
        items:
          type: string
        type: array
      trip_miles:
        format: int32
        type: integer
      user_hid:
        type: boolean
      weight:
        format: nullable
        type: string
      width:
        format: nullable
        type: string
    type: object
  quote_amount:
    format: int32
    type: integer
type: object
x-restlet:
  section: BOARDS
