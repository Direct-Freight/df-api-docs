type: object
properties:
  entry_id:
    type: array
    description: 'A hash with each entry id as the name, and a note_object as the value.'
    items:
      type: string
example: |-
  {
    "entry_id": [
      "entry_id",
      "entry_id"
    ]
  }
x-restlet:
  section: BOARDS
